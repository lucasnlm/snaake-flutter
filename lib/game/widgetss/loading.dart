import 'package:flutter/widgets.dart';

/// Loading width shown while loading.
class Loading extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Loading'),
    );
  }
}
